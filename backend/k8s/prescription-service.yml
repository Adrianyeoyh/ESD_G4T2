apiVersion: apps/v1
kind: Deployment
metadata:
  name: prescription-service
  namespace: clinicflow
  labels:
    app: prescription-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prescription-service
  template:
    metadata:
      labels:
        app: prescription-service
    spec:
      containers:
        - name: prescription-service
          image: backend-prescription_service
          imagePullPolicy: Never
          ports:
            - containerPort: 5005
          env:
            - name: APP_ENV
              value: docker
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: esd_db
            - name: DB_SCHEMA
              value: prescription_schema
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /prescription
              port: 5005
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /prescription
              port: 5005
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: prescription-service
  namespace: clinicflow
  labels:
    app: prescription-service
spec:
  type: ClusterIP
  selector:
    app: prescription-service
  ports:
    - port: 5005
      targetPort: 5005
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: prescription-service-hpa
  namespace: clinicflow
  labels:
    app: prescription-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: prescription-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
